
version: 2
jobs:

  build-on-linux:
    shell: /bin/bash --login
    parallelism: 3
    working_directory: ~/build-on-linux
    environment:
      COVERALLS_REPO_TOKEN: aSN6kpjINvc8uDM9OxnRW0bPcbBbhFLz5
    docker:
      - image: circleci/build-image:ubuntu-14.04-XXL-upstart-1189-5614f37
    steps:
      - checkout
      - run: 
        command: |
          wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh
          chmod +x miniconda.sh
          ./miniconda.sh -b -f
          ${HOME}/miniconda3/bin/conda install --file conda_requirements.txt --yes
          ${HOME}/miniconda3/bin/pip install nose-cov python-coveralls
          ${HOME}/miniconda3/bin/pip install -e .
      - run: ${HOME}/miniconda3/bin/nosetests -v tests --with-coverage --cover-package=factor_analyzer --cov-config .coveragerc

      - run: echo | ls $~/build-on-linux || true
      - run: echo | ls ${HOME} || true
      - run: echo | ls ${HOME}/miniconda3/ || true
      - run: echo | ls ${HOME}/miniconda3/bin || true
      - run: echo | ls ${HOME}/miniconda3/bin || true

workflows:
  version: 2
  test-all-environments:
    jobs:
      - build-on-linux

# - run: cd ${HOME}/miniconda3/bin/coveralls
# - run: cd ${CIRCLE_TEST_REPORTS}/miniconda3/bin/coveralls

# Save test results
# - store_test_results:
#     path: test-results
#     destination: test-results

# Save artifacts
# - store_artifacts:
#     path: test-reports
#     destination: test-reports
    